# Deployment-automation-tool-
In many teams, deployments are still performed manually by copying files and uploading them to production FTP servers. This often leads to issues like wrong file deployment, old files not being removed, or missed steps during urgent releases.  This tool standardizes the workflow. This tool automate the works
